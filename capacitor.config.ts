import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.artesgo.anim',
  appName: 'fitc',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
